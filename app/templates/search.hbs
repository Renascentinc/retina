<div class="search-bar">
	<select class="search-parameter search" id="status">
		<option value=""> Status </option>
		{{#each model.status.status as |status|}}
			<option value={{status}}>
				{{status}}
			</option>
		{{/each}}
	</select>
	
	<select class="search-parameter search" id="type">
		<option>Type</option>
		{{#each model.types.types as |type|}}
			<option value={{type}}>
				{{type}}
			</option>
		{{/each}}
	</select>
	
	<select class="search-parameter search" id="owner">
		<option>Owner</option>
		{{#each model.owners.owners as |owner|}}
			<option value={{owner.userid}}>
				{{owner.username}}
			</option>
		{{/each}}
	</select>
	
		<select class="search-parameter search" id="brand">
		<option>Brand</option>
		{{#each model.brands as |brand|}}
			<option value={{brand}}>
				{{brand}}
			</option>
		{{/each}}
	</select>
	
</div>

<div id="tool-display-box-full">
{{#each model.tools as |tool|}}
	<div class="specs-box">
		<div class="facts">
			{{tool.attributes.brand}} 
			{{tool.attributes.type}}<br>
			{{tool.id}}<br>
			{{tool.attributes.username}} 
		</div>
		
		<div class="options">
			{{#link-to 'info' tool.id}}more 
				<span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span> 
			{{/link-to}}<br>
		</div>
	</div>
{{/each}}
</div>